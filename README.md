# AxSim-GPU
Approximate Simulator on GPU

This repo is about simulator about approximate circuits on GPU. Based on parallel Mont Carlo simulation on GPU, an efficient computation of different error metrics including error rate(ER), maximum error magnitude(MEM), mean relative error distance(MRED), mean square error(MSE) are provided.
